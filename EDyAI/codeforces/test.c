    //meter luego de k
    /*for (int i = k, j = 0; i < longitud && j < longitud - k; i++, j++) {
        pilaCambiada[j] = malloc(sizeof(char) * strlen(pila[i]));
        strcpy(pilaCambiada[j], pila[i]);
    }
    
    //meter final
    for (int i = 0, j = longitud - k; i < k && j < longitud; i++, j++) {
        pilaCambiada[j] = malloc(sizeof(char) * strlen(pila[i]));
        strcpy(pilaCambiada[j], pila[i]);
    }*/
